import BaseObjectClass from './BaseObjectClass';

abstract class PageObjectClass extends BaseObjectClass {
  /**
   * In a concrete page class, define a scopeName, e.g.:
   *   protected scopeName = "login";
   */
}

export default PageObjectClass;
